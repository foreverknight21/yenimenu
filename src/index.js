import React from "react";
import ReactDOM from "react-dom/client";
import DigitalMenu from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <DigitalMenu />
  </React.StrictMode>
);
